apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dev-aws.fullname" . }}-test-connection"
  labels:
    {{- include "dev-aws.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dev-aws.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
