apiVersion: v1
kind: Service
metadata:
  name: {{ include "dev-aws.fullname" . }}
  labels:
    {{- include "dev-aws.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dev-aws.selectorLabels" . | nindent 4 }}
